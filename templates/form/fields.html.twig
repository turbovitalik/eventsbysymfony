{% block form_row %}
    {% spaceless %}
        <div class="form_row blablabla">
            {{ form_label(form) }}
            {{ form_errors(form) }}
            {{ form_widget(form) }}
            {{ form_help(form) }}
        </div>
    {% endspaceless %}
{% endblock form_row %}